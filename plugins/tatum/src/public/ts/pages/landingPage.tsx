import { Guideline } from "../components/guideline";
import { Layout } from "../components";

export const LandingPage = () => {
    return (
        <Layout>
            <Guideline />
        </Layout>
    );
};
